using UnityEngine;
using UnityEditor;

//[CustomEditor(typeof(StartPoint))]
public class QuestEditor : Editor {
	// Add a GameObject box with "Set as End, Set as chackpoint at place X"
	// Remove End and Remove checkpoint
}
